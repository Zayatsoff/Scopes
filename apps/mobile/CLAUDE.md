## Agent skills

### Issue tracker

Issues live on GitHub (`Zayatsoff/Scopes`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles map to identically-named GitHub labels; a separate `WIP` label exists outside the triage state machine. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
