# root-level workspace artifacts; each app/service keeps its own .gitignore too
node_modules/

# yarn (node-modules linker - no need to commit cache/state)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

.DS_Store
